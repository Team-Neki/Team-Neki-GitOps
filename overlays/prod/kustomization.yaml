apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - deployment.yaml
  - notification-deployment.yaml
  - ingress.yaml
  - ingressroute-https.yaml
  - certificate.yaml
  - monitoring.yaml
  - monitoring-ingressroute.yaml
  # sprint (Next.js) - sprint.suitestudy.com
  - sprint-deployment.yaml
  - sprint-configmap.yaml
  - sprint-ingressroute.yaml
  - sprint-certificate.yaml
  # portal 은 별도 레포(Team-Neki-Portal)로 분리 - argocd/apps/portal.yaml Application 이 관리
  # secret.yaml 은 .gitignore 처리 - 민감 정보 별도 관리
  # - secret.yaml
  # tls-secret.yaml 은 cert-manager Certificate로 대체됨 (deprecated)
  # - tls-secret.yaml
